Conditionals

# syntax
if condition:
    code
elif condition:
    code
else:
    code

#//////////////////////////////////////////////////////////////////////////////////////////
#//////////////////////////////////////////////////////////////////////////////////////////

# Exercise 11
#1 Declare a function add_two_numbers. It takes two parameters and it returns a sum.
def add_two_num(num1, num2):
    sum = num1 + num2
    return sum  

print(add_two_num(2,5))

#2 Area of a circle is calculated as follows: area = π x r x r. Write a function that calculates area_of_circle.
def area_of_circle(r):
    pi = 3.14
    area = pi * r ** 2
    return area
print(area_of_circle(2))

#3 Write a function called add_all_nums which takes arbitrary number of arguments and sums all the arguments. Check if
def add_all_nums(*args):
    sum = 0
    for nums in args:
        sum += nums
    return sum
print(add_all_nums(1,2,3,6))

#4 all the list items are number types. If not do give a reasonable feedback.
def add_all_nums(*args):
    sum = 0
    for nums in args:
        if not isinstance(num(int, float)):
            return f'Error: {nums} is not a number'
        sum += nums
    return sum
print(add_all_nums(1,2,3,6))

#5 Temperature in °C can be converted to °F using this formula: °F = (°C x 9/5) + 32. Write a function which converts °1 to °F, convert_celsius_to-fahrenheit.
def convert_cel_fah(c):
    fah = (c * 9/5) + 32
    return fah

print(convert_cel_fah(40))

#6 Write a function called check-season, it takes a month parameter and returns the season: Autumn, Winter, Spring or  Summer.
def check_season(month):
    if month in ['March', 'April', 'May']:
        return 'Autumn'
    elif month in ['June','July','August']:
        return 'Winter'
    elif month in ['September','October','November']:
        return 'Spring'
    elif month in ['December', 'January', 'February']:
        return 'Summar'
    else:
        return 'Not a month'

print(check_season('December'))

#7 Write a function called calculate_slope which return the slope of a linear equation
def cal_slop(x, y, b):
    m = (y - b)/x
    return m
print(cal_slop(1,2,0))

#8 Quadratic equation is calculated as follows: ax² + bx + c = 0. Write a function which calculates solution set of a  quadratic equation, solve_quadratic_eqn.

#9 Declare a function named print_list. It takes a list as a parameter and it prints out each element of the list.